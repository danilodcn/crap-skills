[project]
name = "crap4py"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["radon>=6.0", "coverage>=7.15"]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["scripts"]
testpaths = ["tests"]
